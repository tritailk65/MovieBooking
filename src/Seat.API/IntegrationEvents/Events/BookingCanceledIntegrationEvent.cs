// Legacy Seat-local contract. It is intentionally no longer published because
// cross-service messages must come from SagaOrchestration.Contracts.
//
// using EventBus.Events;
// namespace Seat.API.IntegrationEvents.Events;
// public record BookingCanceledIntegrationEvent(int showtimeId, string userId) : IntegrationEvent;
