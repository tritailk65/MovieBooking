# Static test data

Place non-secret, reusable test datasets in this directory when a scenario
needs fixed inputs. Current booking scenarios generate unique users and
showtimes dynamically to avoid lock collisions.

Never store access tokens, passwords, connection strings, or production user
data here.
